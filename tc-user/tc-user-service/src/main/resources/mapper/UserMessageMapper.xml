<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcyh.user.mapper.UserMessageMapper">

    <select id="selectMessagesBetweenUsers" resultType="com.tcyh.user.entity.UserMessage">
        SELECT * FROM user_message 
        WHERE (sender_id = #{userId1} AND receiver_id = #{userId2}) 
           OR (sender_id = #{userId2} AND receiver_id = #{userId1})
        ORDER BY create_time DESC 
        LIMIT #{limit}
    </select>

    <select id="countUnreadMessages" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user_message 
        WHERE receiver_id = #{userId} AND is_read = 0
    </select>

    <update id="markMessagesAsRead">
        UPDATE user_message 
        SET is_read = 1, read_time = NOW()
        WHERE receiver_id = #{userId} AND sender_id = #{senderId} AND is_read = 0
    </update>

</mapper>
