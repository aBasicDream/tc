<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcyh.user.mapper.UserInfoMapper">

    <select id="selectByUsername" resultType="com.tcyh.user.entity.UserInfo">
        SELECT * FROM user_info WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultType="com.tcyh.user.entity.UserInfo">
        SELECT * FROM user_info WHERE phone = #{phone}
    </select>

    <select id="selectByEmail" resultType="com.tcyh.user.entity.UserInfo">
        SELECT * FROM user_info WHERE email = #{email}
    </select>

</mapper>
