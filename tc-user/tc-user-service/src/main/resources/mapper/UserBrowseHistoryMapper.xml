<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcyh.user.mapper.UserBrowseHistoryMapper">

    <select id="selectBrowseHistoryByUserId" resultType="com.tcyh.user.entity.UserBrowseHistory">
        SELECT * FROM user_browse_history 
        WHERE user_id = #{userId} 
        <if test="contentType != null">
            AND content_type = #{contentType}
        </if>
        ORDER BY create_time DESC 
        LIMIT #{limit}
    </select>

    <delete id="deleteOldBrowseHistory">
        DELETE FROM user_browse_history 
        WHERE user_id = #{userId} 
        AND create_time &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </delete>

</mapper>
