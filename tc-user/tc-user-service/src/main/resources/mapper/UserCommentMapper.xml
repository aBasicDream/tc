<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcyh.user.mapper.UserCommentMapper">

    <select id="selectCommentsByContent" resultType="com.tcyh.user.entity.UserComment">
        SELECT * FROM user_comment 
        WHERE content_id = #{contentId} AND content_type = #{contentType} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectRepliesByParentId" resultType="com.tcyh.user.entity.UserComment">
        SELECT * FROM user_comment 
        WHERE parent_id = #{parentId} AND status = 1
        ORDER BY create_time ASC
    </select>

    <select id="countCommentsByContent" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user_comment 
        WHERE content_id = #{contentId} AND content_type = #{contentType} AND status = 1
    </select>

</mapper>
