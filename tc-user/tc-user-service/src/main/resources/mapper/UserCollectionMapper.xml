<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcyh.user.mapper.UserCollectionMapper">

    <select id="selectCollectionsByUserId" resultType="com.tcyh.user.entity.UserCollection">
        SELECT * FROM user_collection 
        WHERE user_id = #{userId} 
        <if test="contentType != null">
            AND content_type = #{contentType}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectCollectionByUserAndContent" resultType="com.tcyh.user.entity.UserCollection">
        SELECT * FROM user_collection 
        WHERE user_id = #{userId} AND content_id = #{contentId} AND content_type = #{contentType}
    </select>

</mapper>
